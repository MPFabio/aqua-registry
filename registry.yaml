packages:
  - type: http
    name: kubernetes/kubectl
    url: http://host.docker.internal:8081/repository/binaries/kubectl/{{.Version}}/kubectl
    format: raw
    files:
      - name: kubectl

  - type: http
    name: helm/helm
    url: http://host.docker.internal:8081/repository/binaries/helm/{{.Version}}/helm-{{.Version}}-linux-amd64.tar.gz
    format: tar.gz
    files:
      - name: helm
        src: linux-amd64/helm
